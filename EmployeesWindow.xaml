<Window x:Class="UfanetApp.EmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудники - Уфанет" Height="550" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#E3F2FD">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1976D2" Padding="10" CornerRadius="10" Margin="0,0,0,15">
            <TextBlock Text="СПИСОК СОТРУДНИКОВ" FontSize="18" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <Button x:Name="btnHome" Grid.Row="1" Content="НА ГЛАВНУЮ" Width="120" Height="35" 
                HorizontalAlignment="Left" Margin="0,0,0,10" Click="btnHome_Click"
                Background="#1976D2" Foreground="White" FontWeight="Bold"/>

        <DataGrid x:Name="dgEmployees" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single"
                  AlternatingRowBackground="#BBDEFB" FontSize="13"
                  Background="White" BorderBrush="#42A5F5" BorderThickness="2" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding employee_id}" Width="50"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding last_name}" Width="100"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding first_name}" Width="100"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding patronymic}" Width="100"/>
                <DataGridTextColumn Header="Должность" Binding="{Binding position}" Width="120"/>
                <DataGridTextColumn Header="Отдел" Binding="{Binding department}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="150"/>
                <DataGridTextColumn Header="Зарплата" Binding="{Binding salary, StringFormat={}{0:N0}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
            <Button x:Name="btnAdd" Content="ДОБАВИТЬ" Width="110" Height="35" 
                    Margin="0,0,10,0" Click="btnAdd_Click"
                    Background="#66BB6A" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button x:Name="btnEdit" Content="РЕДАКТИРОВАТЬ" Width="120" Height="35" 
                    Margin="0,0,10,0" Click="btnEdit_Click"
                    Background="#FFB74D" Foreground="#333333" FontWeight="Bold" Cursor="Hand"/>
            <Button x:Name="btnDelete" Content="УДАЛИТЬ" Width="100" Height="35" 
                    Click="btnDelete_Click"
                    Background="#EF5350" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button x:Name="btnRefresh" Content="ОБНОВИТЬ" Width="100" Height="35" 
                    Click="btnRefresh_Click"
                    Background="#42A5F5" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

        <Border Grid.Row="4" Background="#1976D2" Padding="8" CornerRadius="5" Margin="0,15,0,0">
            <TextBlock Text="© 2024 Уфанет - Учет сотрудников" 
                       Foreground="#BBDEFB" FontSize="10" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>